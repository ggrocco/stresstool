# This is a configuration file for the stress test LM.

funcs:
  - name: token
    cmd: ["jwt_cli", "7990d6d7f24cc716b2b94e096e264aee", "5eff003d1ee62f5dbb47c2352ea7e47d"]

tests:
  - name: get_order
    path: "https://api-qa.pwnhealth.com/v2/labs/orders/1313105"
    method: "GET"
    requests_per_second: 8
    threads: 10
    run_seconds: 100

    headers:
      Authorization: "{{ token() }}"

    assert:
      status_code: 200
